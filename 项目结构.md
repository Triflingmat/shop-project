client/src/
├── api/ # API 接口层
│ ├── login.ts # 登录接口
│ ├── user.ts # 用户 CRUD
│ ├── goods.ts # 商品 CRUD
│ ├── category.ts # 分类查询
│ ├── order.ts # 订单 CRUD
│ ├── order_item.ts # 订单明细
│ └── aftersale.ts # 售后
├── types/index.ts # 全部数据类型定义
├── utils/
│ ├── request.ts # Axios 封装（拦截器/Token）
│ └── token.ts # JWT 解析与过期检测
├── stores/
│ ├── user.ts # 用户状态（登录/注册/退出）
│ ├── cart.ts # 购物车（Pinia + localStorage 持久化）
│ └── category.ts # 分类状态
├── router/index.ts # 14 条路由 + 路由守卫
├── components/
│ ├── GoodsCard.vue # 商品卡片
│ ├── OrderCard.vue # 订单卡片
│ └── QuantitySelector.vue # 数量选择器
└── views/
├── Layout.vue # 前台布局（导航栏+页脚）
├── home/index.vue # 首页（Banner+热销+分类）
├── login/Login.vue # 登录页
├── register/Register.vue # 注册页
├── goods/
│ ├── GoodsList.vue # 商品列表（分类筛选+搜索+分页）
│ └── GoodsDetail.vue # 商品详情（加购+立即购买）
├── cart/CartPage.vue # 购物车
├── order/
│ ├── OrderConfirm.vue # 订单确认
│ ├── OrderList.vue # 订单列表（状态Tab切换）
│ └── OrderDetail.vue # 订单详情（步骤条）
├── user/UserProfile.vue # 个人中心（编辑信息+改密码）
└── aftersale/
├── AftersaleApply.vue # 申请售后
├── AftersaleList.vue # 售后列表
└── AftersaleDetail.vue # 售后详情
